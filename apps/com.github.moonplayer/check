#!/bin/env python3
import sys
from GithubCheck import check

github = {
    'author': "coslyk",
    'project': "MoonPlayer",
    "suffix": ".AppImage",
    "prefix": ""
}

if __name__ == "__main__":
    sys.exit(check(github, "X86"))
