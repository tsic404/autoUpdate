#!/bin/env python3
import sys
from GithubCheck import check

github = {
    'author': "srevinsaju",
    'project': "guiscrcpy",
    "suffix": "x86_64.AppImage",
    "prefix": ""
}

if __name__ == "__main__":
    sys.exit(check(github, "X86"))
