appid: com.charlesproxy.charles
id: 1001078

binaryPacakge:
  build:
    type: binary
    content: 
      path: /
    entries: 
      applications:
        fix:
          - desktop_fix
      icons:
        fix:
          - icons_fix
      mime:
        fix:
          - mime_fix

debianInfo:
  debian_patched:
    Description: |
      Capture and get the problem of network.
      Charles is a local HTTP proxy and capturing tool.
      It supports SSL proxy, flow control, AJAX, AMF debug, resend network request and modify network request parameters, etc.
      It can check the compatible for HTML, CSS and RSS contents.
